
/**
 * Calculates the sum of two numbers
 */
function add(a, b) {
    // Implement
    return 0;
}

/**
 * Calculates the difference between two numbers
 */
function subtract(a, b) {
    // Implement
    return 0;
}

/**
 * Calculates the product of two numbers
 */
function multiply(a, b) {
    // Implement
    return 0;
}

/**
 * Calculates the quotient of two numbers
 */
function divide(a, b) {
    // Implement
    return 0;
}

/**
 * Calculates the inverse of a number
 *
 * e.g. invert(4) => 0.25
 * e.g. invert(1) => 1
 * e.g. invert(0.5) => 2.0
 *
 */
function invert(a) {
    // Implement
    return 0;
}

/**
 * Calculates the remainder after dividing `a` by `b`
 *
 * e.g. remainder(10, 5) => 0 (2 remainder 0)
 * e.g. remainder(1, 5) => 1 (0 remainder 1)
 * e.g. remainder(5, 2) => 1 (2 remainder 1)
 * e.g. remainder(10, 10) => 0 (1 remainder 0)
 *
 */
function remainder(a, b) {
    // Implement
    return 0;
}


module.exports = {
    add,
    subtract,
    multiply,
    divide,
    invert,
    remainder
}
